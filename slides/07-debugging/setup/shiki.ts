// Adds the course's `pseudocod` grammar. See common/setup/shiki.ts — the langs
// list there REPLACES Slidev's, so every language used in a deck must be in it.
export { default } from '../../../common/setup/shiki'
